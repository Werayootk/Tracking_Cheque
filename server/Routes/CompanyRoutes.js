import express from "express";

const companyRouter = express.Router();

export default companyRouter;

/**
 * 1. GET /api/companies
 * 2. GET /api/companies/:id
 * 3. POST /api/companies/create
 * 4. PUT /api/companies/:id/update
 * 5. DELETE /api/companies/:id/delete
 */
 